<% show_post ||= false %>

<div class="well span8">
		<em>"<%= comment.body%>"</em>
		<% if show_post == true %>
		on <%= link_to comment.post.title, post_path(comment.post)%>
		<br>
		<% end %>
		<span class="quiet">Posted by</span> <%= link_to comment.user.username, user_path(comment.post.user) %>
		<small> <%= date(comment.created_at) %> </small>
		<span>
</div>